services:
  # Database Migration Service
  db-migrator:
    image: mcr.microsoft.com/mssql-tools
    container_name: distribuidora-db-migrator
    depends_on:
      sqlserver:
        condition: service_healthy
    environment:
      - SA_PASSWORD=yourStrongPassword123#
      - DB_SERVER=sqlserver
      - DB_NAME=distribuidora_db
    volumes:
      - ./sql:/docker-entrypoint-initdb.d
    command: ["/bin/bash", "/docker-entrypoint-initdb.d/init-db.sh"]
    networks:
      - distribuidora-network
    restart: "no"  # Run once only

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    container_name: distribuidora-api
    ports:
      - "8000:8000"
    depends_on:
      rabbitmq:
        condition: service_healthy
      sqlserver:
        condition: service_healthy
      db-migrator:
        condition: service_completed_successfully
    environment:
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/
      - DB_SERVER=sqlserver
      - DB_PORT=1433
      - DB_NAME=distribuidora_db
      - DB_USER=SA
      - DB_PASSWORD=yourStrongPassword123#
      - RABBITMQ_HOST=rabbitmq
      - RABBITMQ_PORT=5672
      - RABBITMQ_USER=guest
      - RABBITMQ_PASSWORD=guest
<<<<<<< HEAD
      - DEBUG=True
=======
      - SMTP_SERVER=smtp.gmail.com
      - SMTP_PORT=587
      - SMTP_USER=distribuidoraperrosgatos@gmail.com
      - SMTP_PASSWORD=ximz ubff zigp wnxj
      - EMAIL_FROM_NAME=Soft PetPlace
      - EMAIL_FROM_ADDRESS=distribuidoraperrosgatos@gmail.com
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/docs", "||", "exit", "1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
    restart: unless-stopped
>>>>>>> 125b786d3b1dd8f99495e4149cf969ee3116670b
    networks:
      - distribuidora-network
    volumes:
      - ./backend/api/app/uploads:/app/uploads   # persistir uploads entre reinicios y hacerlo accesible desde contenedor

  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    container_name: distribuidora-worker
    depends_on:
      rabbitmq:
        condition: service_healthy
      sqlserver:
        condition: service_healthy
      db-migrator:
        condition: service_completed_successfully
    environment:
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/
      - SMTP_HOST=smtp.gmail.com
      - SMTP_PORT=587
      - SMTP_USER=distribuidoraperrosgatos@gmail.com
      - SMTP_PASS=ximz ubff zigp wnxj
      - SMTP_PASSWORD=ximz ubff zigp wnxj
      - EMAIL_FROM_NAME=Soft PetPlace
      - EMAIL_FROM_ADDRESS=distribuidoraperrosgatos@gmail.com
      - DB_SERVER=sqlserver
      - DB_PORT=1433
      - DB_NAME=distribuidora_db
      - DB_USER=SA
      - DB_PASSWORD=yourStrongPassword123#
      - RABBITMQ_HOST=rabbitmq
      - RABBITMQ_PORT=5672
      - RABBITMQ_USER=guest
      - RABBITMQ_PASSWORD=guest
    restart: unless-stopped
    networks:
      - distribuidora-network

  rabbitmq:
    image: rabbitmq:3.9-management
    container_name: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 30s
      timeout: 30s
      retries: 3
      start_period: 60s
    restart: unless-stopped
    networks:
      - distribuidora-network

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: sqlserver
    ports:
      - "1433:1433"
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=yourStrongPassword123#
      - MSSQL_PID=Developer
      - MSSQL_AGENT_ENABLED=true
    volumes:
      - sqlserver_data:/var/opt/mssql
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U SA -P 'yourStrongPassword123#' -C -Q 'SELECT 1' || exit 1"]
      interval: 30s
      timeout: 20s
      retries: 10
      start_period: 120s
    restart: unless-stopped
    networks:
      - distribuidora-network

<<<<<<< HEAD
  db-migrator:
    image: mcr.microsoft.com/mssql-tools # This image contains sqlcmd
    container_name: distribuidora-db-migrator
    volumes:
      - ./sql:/sql # Mount the entire sql directory
    command: >
      sh -c "
      echo 'Waiting for SQL Server...';
      for i in $$(seq 1 30); do
        /opt/mssql-tools/bin/sqlcmd -S sqlserver -U SA -P 'yourStrongPassword123#' -Q 'SELECT 1' > /dev/null 2>&1 && break || sleep 5;
      done;
      echo 'Creating database if not exists...';
      /opt/mssql-tools/bin/sqlcmd -S sqlserver -U SA -P 'yourStrongPassword123#' -d master -Q \"IF NOT EXISTS(SELECT * FROM sys.databases WHERE name = 'distribuidora_db') CREATE DATABASE [distribuidora_db];\";
      if [ -f /sql/schema.sql ]; then
        echo 'Applying schema...';
        /opt/mssql-tools/bin/sqlcmd -S sqlserver -U SA -P 'yourStrongPassword123#' -d distribuidora_db -i /sql/schema.sql;
      fi;
      if [ -d /sql/migrations ]; then
        echo 'Applying migrations...';
        for f in /sql/migrations/*.sql; do [ -f \"$$f\" ] && /opt/mssql-tools/bin/sqlcmd -S sqlserver -U SA -P 'yourStrongPassword123#' -d distribuidora_db -i \"$$f\"; done;
      fi;
      if [ -d /sql/seeders ]; then
        echo 'Applying seeders...';
        for f in /sql/seeders/*.sql; do [ -f \"$$f\" ] && /opt/mssql-tools/bin/sqlcmd -S sqlserver -U SA -P 'yourStrongPassword123#' -d distribuidora_db -i \"$$f\"; done;
      fi;
      echo 'Database initialization complete.';
      "
    depends_on:
      sqlserver:
        condition: service_healthy
    networks:
      - distribuidora-network

=======
>>>>>>> 125b786d3b1dd8f99495e4149cf969ee3116670b
volumes:
  sqlserver_data:

networks:
  distribuidora-network:
    driver: bridge